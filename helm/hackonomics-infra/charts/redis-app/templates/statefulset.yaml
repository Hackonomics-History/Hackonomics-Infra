apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "redis-app.fullname" . }}
  labels:
    {{- include "redis-app.labels" . | nindent 4 }}
spec:
  serviceName: {{ include "redis-app.fullname" . }}
  replicas: 1
  selector:
    matchLabels:
      {{- include "redis-app.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "redis-app.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: redis
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - redis-server
            - --appendonly
            - "yes"
            - --dir
            - /data
          ports:
            - name: redis
              containerPort: 6379
              protocol: TCP
          volumeMounts:
            - name: data
              mountPath: /data
          livenessProbe:
            exec:
              command:
                - redis-cli
                - ping
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - redis-cli
                - ping
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "redis-app.fullname" . }}-data
