apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgres-go.fullname" . }}
  labels:
    {{- include "postgres-go.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "postgres-go.selectorLabels" . | nindent 4 }}
  ports:
    - name: postgres
      port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
