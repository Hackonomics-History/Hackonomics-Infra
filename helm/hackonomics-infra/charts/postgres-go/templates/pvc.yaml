apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "postgres-go.fullname" . }}-data
  labels:
    {{- include "postgres-go.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.storage.storageClass }}
  resources:
    requests:
      storage: {{ .Values.storage.size }}
