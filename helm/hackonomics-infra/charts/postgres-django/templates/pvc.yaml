apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "postgres-django.fullname" . }}-data
  labels:
    {{- include "postgres-django.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.storage.storageClass }}
  resources:
    requests:
      storage: {{ .Values.storage.size }}
