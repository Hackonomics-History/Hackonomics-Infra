{{- $ns := .Values.namespace }}
{{- $ri := .Values.refreshInterval }}
{{- $secrets := list
  (dict "name" "hackonomics-auth-env"    "kvPath" "hackonomics/auth")
  (dict "name" "hackonomics-kotlin-env"  "kvPath" "hackonomics/kotlin")
  (dict "name" "hackonomics-fastapi-env" "kvPath" "hackonomics/fastapi")
  (dict "name" "hackonomics-infra-env"   "kvPath" "hackonomics/infra")
}}
{{- range $secrets }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .name }}
  namespace: {{ $ns }}
spec:
  refreshInterval: {{ $ri | quote }}
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: {{ .name }}
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: {{ .kvPath | quote }}
{{- end }}
